{% extends 'base-front.html.twig' %}
{% block body %}
<div class="section">
	<div class="container">
		<div class="row">
        {% for c in categorie %}
            <div class="col-md-4 col-xs-6">
	            <div class="shop">
		            <div class="shop-img">
			            <img src="./img/famille.jpg" alt="">
	                </div>
		            <div class="shop-body">
			            <h3>{{c.nomC}}</h3>
				        <a href="{{ path ('atrcat',{id:c.id})}}" class="cta-btn">Shop now <i class="fa fa-arrow-circle-right"></i></a>
		            </div>
	            </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>            
{% endblock %}